# Meta accounts map

Human-readable map of brand ↔ configured `account_id` (no tokens).
Live credentials live in local `.env` (`META_ACCOUNTS` or `META_ACCOUNTS_FILE`).

| account_id | Brand | FB Page | IG handle | Notes |
|------------|-------|---------|-----------|-------|
| soulplus | SoulPlus AI | | | Product brand |
| maria-lit | Maria Lit | | | Founder |
| soul-healing-center | Soul Healing Center | | | |

Fill handles/Page names when available. Add rows for future brands; mirror the same `id` in `META_ACCOUNTS`.
