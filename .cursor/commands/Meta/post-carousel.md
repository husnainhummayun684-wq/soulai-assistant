Publish an Instagram **carousel** (2–10 items) on a named account.

1. Explicit publish + **account_id**.
2. Collect 2–10 public HTTPS URLs + caption.
3. Preview, then `ig_publish_carousel` with `account_id` and `confirm=true`.
4. Confirm result. Never auto-publish.
