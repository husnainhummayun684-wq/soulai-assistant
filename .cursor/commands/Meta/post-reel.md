Publish an Instagram **Reel** on a named account.

1. Explicit publish + **account_id**.
2. Public HTTPS `video_url` (+ optional cover, caption).
3. Preview; processing can take minutes.
4. `ig_publish_reel` with `account_id` and `confirm=true`.
5. Respect 25 posts / 24h per IG account.
