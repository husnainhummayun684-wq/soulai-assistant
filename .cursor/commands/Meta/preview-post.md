Preview a post for Instagram or Facebook **without publishing**.

1. If multiple Meta accounts are configured, resolve **account_id** (`meta_list_accounts` if unclear). With a single account, omit it.
2. Collect: platform, media type, public HTTPS URLs, caption/message, optional FB schedule time.
3. Call `meta_preview_post`.
4. Show the planned payload. Do **not** publish.
