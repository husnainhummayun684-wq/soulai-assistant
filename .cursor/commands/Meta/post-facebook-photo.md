Publish a **Facebook Page photo** on a named account.

1. Explicit publish/schedule + **account_id**.
2. Public image URL + optional caption; optional `scheduled_publish_time`.
3. Preview, then `fb_publish_photo` with `account_id` and `confirm=true`.
4. Confirm result.
