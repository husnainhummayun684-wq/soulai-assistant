Publish a **Facebook Page video** on a named account.

1. Explicit publish/schedule + **account_id**.
2. Public `file_url` / video URL + optional title/description; optional schedule.
3. Preview (media_type VIDEO), then `fb_publish_video` with `account_id` and `confirm=true`.
4. Confirm result.
