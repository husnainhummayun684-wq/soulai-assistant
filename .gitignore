# Secrets — never commit
.env
.env.local
.env.*.local
.meta-accounts.local.json
*.pem
*.key
credentials.json
secrets/

# OS / editor
.DS_Store
Thumbs.db
Desktop.ini
.vscode/
!.vscode/extensions.json
*.swp
*~

# Cursor — keep shared project MCP/rules/skills in git (Notion token stays in local `.env` only)
!.cursor/mcp.json
!.cursor/rules/
!.cursor/skills/
!.cursor/commands/
!.cursor/mcp-servers/
!.cursor/run-mcp.mjs

# Node / Python (future tooling)
node_modules/
dist/
build/
__pycache__/
*.pyc
.venv/
venv/

# Logs / temp
*.log
logs/*.jsonl
tmp/
temp/
.cache/
